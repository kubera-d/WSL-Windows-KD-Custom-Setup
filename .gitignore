# Build outputs and per-user files (never commit personal settings or project lists)
*.exe
*.ico
devcontrol/src/*.png
settings.json
projects.json
presets.json
*.log
*.bak
helper-config.json
clipboard.png

# Secrets and credentials - never publish
.env
.env.*
!.env.example
*.pem
*.key
*.pfx
*.p12
id_rsa*
*credentials*
*secret*
.git-credentials

# Claude Code transcripts / local data
*.jsonl

# Local Claude Code / editor state
.claude/settings.local.json
.vscode/
